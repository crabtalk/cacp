//! Nothing to see here — the crate exists for [`tests/`](../tests), which run
//! cacp against the reference implementation over a real connection. It is not
//! published, so the reference implementation stays out of cacp's own builds.
